spring.application.name=ecommerce

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/ecommerce_db
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=none

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# JWT

jwt.secret=${JWT_SECRET}
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# R2 Storage
storage.endpoint=${STORAGE_ENDPOINT}
storage.access-key=${STORAGE_ACCESS_KEY}
storage.secret-key=${STORAGE_SECRET_KEY}
storage.bucket=${STORAGE_BUCKET:minimoda-images}
storage.public-url=${STORAGE_PUBLIC_URL}

# Mercado Pago
mercadopago.access-token=${MP_ACCESS_TOKEN}
mercadopago.back-url.success=http://127.0.0.1:5500/orders.html
mercadopago.back-url.failure=http://127.0.0.1:5500/cart.html
mercadopago.notification-url=http://localhost:8080/payments/webhook